<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Console background with scanline effect -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/surface_console"/>
            <stroke android:width="1dp" android:color="@color/border_neon"/>
            <corners android:radius="4dp"/>
        </shape>
    </item>
</layer-list>
