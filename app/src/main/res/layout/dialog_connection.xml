<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/glass_modal_bg"
    android:padding="32dp">

    <!-- Connection Icon -->
    <ImageView
        android:id="@+id/iconConnection"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@android:drawable/ic_menu_preferences"
        android:tint="@color/glass_primary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Title -->
    <TextView
        android:id="@+id/textTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Connect Flipper Zero"
        android:textSize="24sp"
        android:textColor="@color/text_primary"
        android:fontFamily="sans-serif-medium"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/iconConnection"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Description -->
    <TextView
        android:id="@+id/textDescription"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Connect your Flipper Zero via USB-C or Bluetooth to unlock all pentesting tools"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        android:gravity="center"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/textTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Connection Status Indicator -->
    <LinearLayout
        android:id="@+id/layoutStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="16dp"
        android:background="@drawable/glass_card_bg"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/textDescription">

        <View
            android:id="@+id/statusIndicator"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/status_indicator"/>

        <TextView
            android:id="@+id/textStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Not Connected"
            android:textColor="@color/text_tertiary"
            android:textSize="14sp"
            android:layout_marginStart="12dp"/>
    </LinearLayout>

    <!-- Connection Method Buttons -->
    <LinearLayout
        android:id="@+id/layoutButtons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/layoutStatus">

        <Button
            android:id="@+id/btnConnectUSB"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Connect via USB-C"
            android:textColor="@color/text_primary"
            android:background="@drawable/glass_button_primary_bg"
            android:textAllCaps="false"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"/>

        <Button
            android:id="@+id/btnConnectBluetooth"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Connect via Bluetooth"
            android:textColor="@color/text_primary"
            android:background="@drawable/glass_button_bg"
            android:textAllCaps="false"
            android:textSize="16sp"
            android:layout_marginTop="12dp"
            android:fontFamily="sans-serif-medium"/>

        <Button
            android:id="@+id/btnContinueOffline"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Continue Without Device"
            android:textColor="@color/text_tertiary"
            android:background="@android:color/transparent"
            android:textAllCaps="false"
            android:textSize="14sp"
            android:layout_marginTop="12dp"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
