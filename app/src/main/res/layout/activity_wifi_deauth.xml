<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/terminal_black">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@drawable/bg_terminal_header"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_back"
            android:contentDescription="Back" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="WIFI DEAUTH ATTACK"
            android:textColor="@color/terminal_red"
            android:textSize="18sp"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:layout_marginStart="16dp" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Warning -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                app:cardBackgroundColor="@color/terminal_red"
                app:cardElevation="4dp"
                app:cardCornerRadius="8dp">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="16dp"
                    android:text="⚠ WARNING: Only use on networks you own or have permission to test!"
                    android:textColor="@color/white"
                    android:textStyle="bold"
                    android:fontFamily="monospace" />
            </com.google.android.material.card.MaterialCardView>

            <!-- Scan Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnScan"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="SCAN FOR NETWORKS"
                android:backgroundTint="@color/terminal_green"
                android:textColor="@color/terminal_black"
                android:fontFamily="monospace"
                android:layout_marginBottom="16dp" />

            <!-- Network List -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                app:cardBackgroundColor="@color/terminal_dark_gray"
                app:cardElevation="4dp"
                app:cardCornerRadius="8dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="16dp">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="NETWORKS"
                        android:textColor="@color/terminal_green"
                        android:textStyle="bold"
                        android:fontFamily="monospace"
                        android:layout_marginBottom="8dp" />

                    <ListView
                        android:id="@+id/networkList"
                        android:layout_width="match_parent"
                        android:layout_height="200dp"
                        android:background="@color/terminal_black"
                        android:divider="@color/terminal_green"
                        android:dividerHeight="1dp" />
                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <!-- Configure Attack Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnConfigureAttack"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="CONFIGURE ATTACK PARAMETERS"
                android:backgroundTint="@color/terminal_yellow"
                android:textColor="@color/terminal_black"
                android:fontFamily="monospace"
                android:enabled="false"
                android:layout_marginBottom="8dp" />

            <!-- Deauth Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDeauth"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="START DEAUTH ATTACK"
                android:backgroundTint="@color/terminal_red"
                android:textColor="@color/white"
                android:fontFamily="monospace"
                android:enabled="false"
                android:layout_marginBottom="8dp" />

            <!-- Stop Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnStop"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="⏹ STOP ATTACK"
                android:backgroundTint="@color/status_disconnected"
                android:textColor="@color/white"
                android:fontFamily="monospace"
                android:enabled="false"
                android:layout_marginBottom="16dp" />

            <!-- Log Output -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardBackgroundColor="@color/terminal_black"
                app:cardElevation="4dp"
                app:cardCornerRadius="8dp">

                <ScrollView
                    android:layout_width="match_parent"
                    android:layout_height="300dp"
                    android:padding="16dp">

                    <TextView
                        android:id="@+id/logOutput"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="Ready to scan WiFi networks...\n\nRequirements:\n- Monitor mode enabled\n- OR ESP32 Marauder on Flipper\n- OR AWOK ESP32 board"
                        android:textColor="@color/terminal_green"
                        android:fontFamily="monospace"
                        android:textSize="12sp" />
                </ScrollView>
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>
    </ScrollView>
</LinearLayout>
