<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Terminal Noir Cyberpunk Theme -->
    <style name="Base.Theme.PentestDashboard" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Background -->
        <item name="android:windowBackground">@color/background_primary</item>
        <item name="android:colorBackground">@color/background_primary</item>
        <item name="colorSurface">@color/surface_default</item>
        <item name="colorSurfaceVariant">@color/surface_elevated</item>

        <!-- Primary Color (Neon Green) -->
        <item name="colorPrimary">@color/neon_green</item>
        <item name="colorPrimaryVariant">@color/neon_cyan</item>
        <item name="colorOnPrimary">@color/background_primary</item>

        <!-- Secondary Color (Cyan) -->
        <item name="colorSecondary">@color/neon_cyan</item>
        <item name="colorSecondaryVariant">@color/neon_purple</item>
        <item name="colorOnSecondary">@color/background_primary</item>

        <!-- Error Color (Neon Red) -->
        <item name="colorError">@color/neon_red</item>
        <item name="colorOnError">@color/text_primary</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColorTertiary">@color/text_tertiary</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/background_primary</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/background_primary</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="Theme.PentestDashboard" parent="Base.Theme.PentestDashboard" />
</resources>
